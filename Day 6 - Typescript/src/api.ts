import { /* TODO: import User type */ } from "./types";

const API_URL = "https://jsonplaceholder.typicode.com/users";

export async function fetchUsers() {
  // TODO:
  // 1. Add return type (Promise<User[]>)
  // 2. Fetch data
  // 3. Avoid using 'any'
  // 4. Handle errors properly
}